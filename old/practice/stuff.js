var arrayLength = (arr) => (
    `array length = ${arr.length}`
);

var adder = (a,b) => (
    `sum = ${a+b}`
);

var pi = 3.14;

module.exports = {
    arrayLength: arrayLength,
    adder : adder,
    pi : pi,
};